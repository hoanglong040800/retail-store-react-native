name: Deploy

on:
  push:
    branches:
      - main
      - RSP-12-setup-ci-cd

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🔀
        uses: actions/checkout@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}

      - name: Install 📦
        run: yarn

      - name: Deploy 🚀
        run: yarn deploy
