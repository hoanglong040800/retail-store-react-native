name: Deploy

on:
  push:
    branches:
      - main
      - RSP-12-setup-ci-cd

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🔀
        uses: actions/checkout@v3

      - name: Install 📦 
        run: yarn

      - name: Deploy 🚀
        run: yarn deploy