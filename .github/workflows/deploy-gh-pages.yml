name: Deploy

on:
  push:
    branches:
      - main
      - RSP-12-setup-ci-cd

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🔀
        uses: actions/checkout@v4

      - name: Setup git 🛠️
        run:  |
          git config --global user.email "hoanglong040800@gmail.com"
          git config --global user.name "hoanglong040800"

      - name: Install 📦
        run: yarn

      - name: Deploy 🚀
        run: yarn deploy
