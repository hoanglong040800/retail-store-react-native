name: Deploy

on:
  push:
    branches:
      - main
      - RSP-12-setup-ci-cd

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout 🔀
        uses: actions/checkout@v4

      - name: Install 📦
        run: yarn

      - name: Deploy 🚀
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        run: yarn deploy
