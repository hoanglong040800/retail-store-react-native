name: Deploy

on:
  push:
    branches:
      - main
      - RSP-12-setup-ci-cd

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    uses: actions/checkout@v4
    with:
      github_token: ${{ secrets.GITHUB_TOKEN }}

    steps:        
      - name: Install 📦
        run: yarn

      - name: Deploy 🚀
        run: yarn deploy
